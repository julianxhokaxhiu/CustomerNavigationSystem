<!DOCTYPE html>
<html>
<head>
	<title>[{{ app.customer }}]&lt;Listing&gt;</title>
	<link href="{{ app.basePath }}/css/normalize.css" rel="stylesheet">
	<link href="{{ app.basePath }}/css/style.css" rel="stylesheet">
</head>
<body>
	<ul>
	{% for item in app.items %}
		<li>
			{% set postfix = ('.' in item) ? '' : '/' %}
			<a href="{{ item ~ postfix }}">{{ item }}</a>
		</li>
	{% endfor %}
	</ul>
</body>
</html>